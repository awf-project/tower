//! `tower` binary entrypoint.
//!
//! Stub until spec 10 wires the MCP stdio adapter. Kept minimal so the
//! workspace builds clean (spec AC1).

fn main() {
    println!("tower core engine — not yet implemented");
}

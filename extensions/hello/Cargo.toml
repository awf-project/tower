[package]
name = "hello_extension"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Minimal lazy hello extension (spec 26) — reference example for the extension system"

[[bin]]
name = "hello_extension"
path = "src/main.rs"

[dependencies]
extension_protocol = { path = "../../crates/extension_protocol" }
serde_json = "1"
